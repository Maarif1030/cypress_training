/// <reference types="cypress" />

describe("login, dropdown, add element, loop through images ", () => {
  before(() => {
    cy.visit("/login");
  });

  it("verify login with valid credentials", () => {
    //cy.get(".")
  });
});
